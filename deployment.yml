apiVersion: apps/v1
kind: Deployment
metadata:
  name: final-deployment
  # labels:
  #   app: final-pod
spec:
  replicas: 5
  selector:
    matchLabels:
      # app: final-pod
      tag: final-pod
  template:
    metadata:
      labels:
        # app: final-pod
        tag: final-pod
    spec:
      containers:
      - name: final-container
        image: final:v1
        # ports:
        # - containerPort: 80
